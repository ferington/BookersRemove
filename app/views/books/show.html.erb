<h2>Title</h2>
<p><%= @book.title %></p>

<h2>Bory</h2>
<p><%= @book.body %></p>

<%= link_to "編集", edit_book_path(@book.id) %>
<%= link_to "削除", book_path(@book.id), method: :delete, "data-confirm" => "削除しますか?" %>